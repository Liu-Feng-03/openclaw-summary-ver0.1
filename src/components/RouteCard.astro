---
import { withBase } from '../utils/links';
const { item } = Astro.props;
---
<a class="route-card" href={withBase(item.href)}>
  <strong>{item.title}</strong>
  <p class="section-intro">{item.description}</p>
</a>
